# Supabase project URL (Settings -> API -> Project URL)
VITE_SUPABASE_URL=
# Supabase anon/public key (Settings -> API -> anon key)
VITE_SUPABASE_ANON_KEY=
# Storage bucket name (create a PRIVATE bucket in Supabase Storage, and set RLS policies)
VITE_SUPABASE_BUCKET=documents